# Tools

Standalone single-file HTML tools for requirements ⇔ design traceability work.

将来の自動化方針に関する検討メモは`design_notes/`を参照（未実装・未決定の設計議論）。

- `spec_to_json_conversion_tool_v1.18.html`: 仕様書PDF→JSON変換・確認ツール v1.18（取扱説明書準拠UI版）。PDF仕様書から照合用JSONを作成し、様式登録・版間比較・タグ付けを行う。v1.16で確認状態の色分け表示を追加、v1.17でツールバー初期化不具合（作業中JSON・確認済みJSON・検証結果・文書版情報・一括確認・元に戻す・作業復旧ボタンが消える）を修正、v1.18で同梱の様式プロファイル仕様書・生成AI用作成プロンプトをより詳細な版へ更新（ツール内「AIで様式作成」から参照可能）。
- `excel_to_json_conversion_tool_v2.0.8.html`: Excel → JSON変換・確認ツール v2.0.8。Excelの設計検証項目一覧をJSONに変換し、様式定義・タグ辞書に基づき検証する。v2.0.2で確認状態の色分け表示を追加、v2.0.3で「様式・タグ」メニューに「AIで様式作成」を追加してプロファイル仕様書・生成AI用プロンプトを参照可能にし、モーダルの無地ボタンが見えなくなる不具合を修正、v2.0.4でヘッダー・工程バー・「目的から操作を開始する」カードのUI構成を仕様書PDF→JSONツールに近づけ、v2.0.5で配色・タイポグラフィ・ボタン/バッジ/カード/モーダルのデザイントークンを仕様書PDF→JSONツールと統一、v2.0.6で手動タグ付与・変更時に未確認行を自動的に確認済みへ変更するよう修正、v2.0.7で「使い方」を仕様書PDF→JSONツール同様のナビ付きガイド（6項目）へ拡充、v2.0.8で列の値を照合タグへ一括反映する機能と、検索・フィルタ結果を一括確認済みにする機能を追加。
- `json_ab_trace_matching_tool_v12.1.15.html`: JSON A/Bトレース照合ツール v12.1.15。上記2ツールで生成したJSON（要求側A／設計側B）を照合し、ナレッジグラフ・トレースマトリクスを生成する。v12.1.4で入力差替え時の照合キー再検証・詳細表全件フィルタ・重複ID分離等を改善、v12.1.6で詳細テーブル/トレースマトリクス/ナレッジグラフの配色をJSON A（青）・JSON B（橙）・照合情報（緑）で統一し、組み込みプロファイルを汎用の1件に整理、v12.1.7でJSON A表示名・ナレッジグラフの要求側ノードラベルが「[object Object],[object Object]」になる不具合（配列・オブジェクト値をフィールド選定ロジックが誤って文字列化していたため）と、v12.1.6で取り残されていた版数表記（内蔵ヘルプ・生成コード・RO-Crate/ReqIF/Excel/学習データの出力メタデータ）を修正、v12.1.8で詳細テーブルの列過多・行の縦伸びと、ナレッジグラフのノードラベル判読性・選択ノード詳細の生JSON表示を改善（元データ由来の自動抽出列・重複列を既定非表示化しトグルで復元可能に、長文セルを既定3行折り畳み＋クリックで全文表示に、ノードラベルをtrace_title等の短い項目優先に、選択ノード詳細を種別・本文・出典・タグ等の要約表示にしトグルで生JSON表示に切替可能に）、v12.1.9で詳細テーブルの照合情報領域からレビュー内訳系の列（自動照合件数・有効照合件数・人手確定件数・要確認件数・除外件数・除外一覧・レビュー判定内訳・レビューコメント・共有タグ・判定理由）を既定で非表示にし、照合件数・信頼度・照合キー・照合根拠が一目でわかる構成に整理（「レビュー関連の列も表示」トグルで復元可能）、v12.1.10で詳細テーブル（A基準・B基準とも）に「最大信頼度の照合先」列を追加し、照合方式を出さずに最大信頼度がどのA-B対応の結果かを集計ビューのまま直接読み取れるようにした、v12.1.11で1件に複数件が対応する列（表示名一覧・キー一覧・照合根拠）を「/」区切り1行表示から改行＋「1. 」形式の番号付き複数行表示に変更（1件のみの場合は番号なし）、v12.1.12で詳細テーブルの列順を「表示名・照合件数・表示名一覧・最大信頼度・最大信頼度の照合先・照合根拠」に整理し、照合JSON Bキー一覧とB基準のJSON Bキー・補助表示値を既定非表示にした（「元データの詳細列も表示」で復元可能）、v12.1.13で外部AIレビュー指摘を反映し、詳細テーブルのExcel出力で絞り込み結果が0件のときに全件出力されてしまう不具合（空のSetを「フィルタなし」と誤判定）と、JSON B基準・概要グラフでフィルタ適用後に関係線が常に非表示になる不具合（概要グラフのエッジがA→B固定で標準グラフのB基準時source/target入替規約と不整合だった）を修正し、ダウンロードファイル名に残っていた旧版数のアンダースコア表記（V12_1_2/V12_1_3）をV12.1.13へ統一した、v12.1.14で「詳細テーブル」を「照合結果一覧」に改称（タブ名・見出し・列凡例・Excel出力のシート名/ファイル名・ステータス文言まで統一）し、ナレッジグラフのノード色・グループ枠色を照合結果一覧／トレースマトリクスと同じ配色（JSON A=青、JSON B=橙）に統一した（関係線の信頼度による緑/黄/赤の色分けは弱い照合を見つけやすくする指標として維持）、v12.1.15で既定の照合キー選定が、id/trace_id等の文書ごとに独立生成される不透明な識別子（ハッシュ値）を「コードらしい列」として誤って選び、A/B双方で無関係なハッシュ同士の偶然の部分一致が高信頼度スコアになって照合結果が意味不明になる不具合を修正した（id/trace_id/stable_uid/stable_key/*_hash等の名前パターンを持つ列をcode役フィールドの候補から除外。簡易版v1.2で先行して適用していた修正をフル版にも反映）。
- `json_ab_trace_matching_tool_lite_v1.5.html`: JSON A/Bトレース照合ツール（簡易版）v1.5。`json_ab_trace_matching_tool_v12.1.14.html`をベースに、JSON A/Bを読み込んで「読み込み・照合」1回の操作で自動照合結果を確認する用途に絞った簡易ツール。維持しているのはフル版と同じ自動照合エンジン（信頼度ルールは既定値で内部固定、UIでの編集は不可）、入力JSON表（読取専用ビューア）、照合結果一覧（A基準/B基準切替、キー・照合件数・キー一覧・最大信頼度・最大信頼度の照合キー・照合根拠の6列固定表示、全列検索、0.5〜1.0の最小信頼度スライダー1つで一覧とナレッジグラフを同時に絞り込み）、ナレッジグラフ（グラフ粒度＝標準＋グループ枠／概要グラフの切替、自動整列＝二列整列／階層／力学／格子／円形の切替、基準切替、JSON A=青／JSON B=橙のノード配色と信頼度別エッジ配色を維持したSVG保存）、RO-Crate 1.1 ZIP出力、簡易HELP。フル版から削除したのは、照合ロジック設定（照合キー選択を除く）・トレースマトリクス・学習（フィードバック）・モデルテキスト出力（SysML/ReqIF）の4タブ、Excel出力一式（xlsxライブラリ依存ごと排除）、人手レビュー・手動関係追加/付け替え・編集モード・照合プロファイル選択UI、グラフ保存(JSON)/保存済みグラフ読込、詳細列表示切替トグルなど、高度な設定・レビュー・学習機能全般。v1.1で照合結果一覧の「キー」列（旧: 表示名）を、項目の表示名ではなく照合に使われたタグ・一致語句を表示するよう変更した（JSON A/Bキー＝基準側項目自身のタグ、キー一覧＝各照合の一致語句・タグ、最大信頼度の照合キー＝最大信頼度を出した照合の一致語句。タグがない項目のみ表示名にフォールバック）、v1.2で既定の照合キー選定が、id/trace_id等の文書ごとに独立生成される不透明な識別子（ハッシュ値）を「コードらしい列」として誤って選び、A/B双方で無関係なハッシュ同士の偶然の部分一致が高信頼度スコアになって照合結果一覧が意味不明になる不具合を修正した（id/trace_id/stable_uid/stable_key/*_hash等の名前パターンを持つ列をcode役フィールドの候補から除外）、v1.3でフル版「照合ロジック設定」タブから「照合キー選択」機能のみを復活させ、照合結果一覧タブの折りたたみセクションからJSON A/Bの任意キーをペア指定・追加・削除・有効/無効切替できるようにした（「再照合」ボタンがないため変更は即座に一覧・グラフへ反映、「標準設定に戻す」もv1.2の不透明ID除外ガード適用後の既定値を使用）、v1.4でナレッジグラフのノード・エッジが力学レイアウト固定のため重なって見づらいという指摘を受け、フル版と同じグラフ粒度・自動整列の切替ボタン行を復活させた（休眠していたrunGraphLayout/computeTwoColPositions等の処理ロジックにHTML要素を追加しただけで動作。ノード数に応じた標準⇔概要の自動切替は維持しつつ手動切替も可能に）、v1.5で照合キー設定の変更のたびに即座に一覧・グラフを再計算する挙動が不安定に感じられるとの指摘を受け、フル版と同じ「🔄 再照合して反映」ボタンを追加し、キーペアの有効/無効切替・変更・追加・削除・標準設定復元はいずれも即座の再計算を行わずテーブル表示のみ更新、実際の反映は再照合ボタンを押した時点でまとめて行うよう変更した。
- `usage_guide.md`: PDF仕様書とExcel設計項目のトレース照合ツール群 利用方法・対応範囲・運用仕様。マトリクス形式のExcel（行=設計項目、列=標準機種／顧客対応など）を想定したユースケースを前提に、各ツールの役割・照合の粒度（PDF側は章・節・段落単位、Excel側は行単位）・運用上の注意点・品質確認チェックリストをまとめたガイド。
